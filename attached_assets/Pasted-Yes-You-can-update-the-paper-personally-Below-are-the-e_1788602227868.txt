Yes. You can update the paper personally. Below are the exact locations and shorter replacement text. Replace only the incorrect sentences; leave the rest of each paragraph unchanged.

## 1. Chapter 1 — Introduction

Find the sentence that says users can:

> enter the total weight and size breakdown of their calamansi, choose a prediction algorithm, and get an immediate predicted juice yield.

Replace it with:

> Users can enter the total weight of their calamansi batch in kilograms or grams. The system automatically applies the Simple Linear Regression model using weight only and provides an immediate predicted juice yield.

---

## 2. Chapter 1 — Purpose and Description

Find the sentence beginning:

> The result is a web-based system built with Python and Streamlit where users can enter...

Replace that sentence with:

> The result is a web-based system built with Python and Streamlit where regular users enter the total calamansi batch weight in kilograms or grams. The system converts the value to grams and automatically applies the Simple Linear Regression model.

Leave the sentences about avoiding ripeness classification and image processing unchanged.

---

## 3. Chapter 1 — Objectives of the Study

Find the objective beginning:

> To design a fully functional web-based prediction system using Python and Streamlit that accepts user inputs for total weight...

Replace the entire objective with:

> To design a fully functional web-based prediction system using Python and Streamlit that accepts the total calamansi batch weight in kilograms or grams, automatically applies Simple Linear Regression for regular-user predictions, displays the predicted juice yield in milliliters and liters, records prediction history, and allows administrators to review all three trained regression models.

---

## 4. Chapter 1 — Scope and Limitations

Find the sentence that says:

> Users enter the total batch weight in kilograms, which the system internally converts to grams before processing.

Replace it with:

> Regular users enter the total batch weight in kilograms or grams, and the system converts the value to grams before applying the Simple Linear Regression model.

Add this sentence after it:

> The size feature remains part of the original training data and administrator model analysis, but regular users are not required to enter size counts.

---

## 5. Chapter 1 — Usability

Find the sentence that says:

> The interface was kept straightforward so that users can input the total weight and size distribution of a calamansi batch...

Replace it with:

> The interface was kept straightforward so that users can enter the total batch weight in kilograms or grams and receive an immediate predicted juice yield without requiring specialized knowledge, size classification, or algorithm selection.

---

## 6. Chapter 3 — System Description

Find the paragraph that says the system is based on:

> two input variables: the total weight of the batch... and the count of small, medium, and large calamansi.

Replace that part with:

> For regular-user predictions, the system uses the total batch weight as the only required input. The weight may be entered in kilograms or grams and is converted to grams before being passed to the Simple Linear Regression model. The original size feature remains available for the training and administrator model-analysis functions.

---

## 7. Chapter 3 — Peopleware: User

Find the paragraph beginning:

> User (Farmer / Vendor / Juice Processor) refers to the primary end users...

Replace the paragraph with:

> User (Farmer / Vendor / Juice Processor) refers to the primary end users of the system. After logging in, regular users enter the total weight of their calamansi batch in kilograms or grams. The system automatically applies Simple Linear Regression and provides the predicted juice yield. Regular users can also view and delete their own prediction history.

---

## 8. Chapter 4 — Requirements

Find the sentence that says:

> Functional requirements include allowing users to enter the total weight in kilograms, size breakdown, and preferred algorithm...

Replace it with:

> Functional requirements include allowing regular users to enter the total weight in kilograms or grams, converting the value to grams, automatically applying Simple Linear Regression, displaying the predicted juice yield, and saving the result to the user’s prediction history. Administrators can manage users, view all prediction records, and review all three trained models.

---

## 9. Chapter 4 — Use Case Description

Find the paragraph beginning:

> The diagram shows that the user can perform key system functions starting with entering the total weight and size distribution...

Replace it with:

> The regular-user workflow begins when the user logs in and enters the total calamansi batch weight. The user selects kilograms or grams, and the system converts the value to grams before applying the Simple Linear Regression model. The predicted juice yield is displayed in milliliters and liters and saved to the user’s prediction history. Regular users are not required to enter size counts or select an algorithm.

Leave the administrator use-case description, but make sure it says that the administrator can review all three model results.

---

## 10. Chapter 4 — Description of the Prototype: Input Module

Find the paragraph beginning:

> Input Module – The system presents users with an input form consisting of three primary fields...

Replace it with:

> Input Module – The regular-user input form accepts the total calamansi batch weight and its unit of measurement. Users can select kilograms or grams. The system validates the weight, converts it to grams, and automatically applies the Simple Linear Regression model.

---

## 11. Chapter 4 — Data Preprocessing

Find the sentence that says:

> A weighted average size score is computed from the calamansi counts...

Replace it with:

> For regular-user predictions, no weighted average size score is required because the Simple Linear Regression model uses weight only. The entered weight is converted to grams before prediction.

Keep the following explanation about Multiple Linear Regression and Polynomial Regression, but add:

> These two models continue to use weight and size during training and administrator model analysis.

---

## 12. Testing Table

In the **Input Validation Test** row, replace:

> Submit inputs with missing fields, zero weights, and invalid size counts.

With:

> Submit missing, zero, negative, and out-of-range weights using both kilogram and gram units.

In the **Model Integration Test** row, replace the test for selecting all three algorithms with:

> Submit valid weights in kilograms and grams and verify that the values are converted correctly and processed by the Simple Linear Regression model.

Add an administrator test row:

> **Administrator Model Analysis Test** — Verify that administrators can access the performance results of Simple Linear Regression, Multiple Linear Regression, and Polynomial Regression.

## Do not change these sections yet

Leave these unchanged for this user-flow update:

- Training methodology
- Three regression model descriptions
- Dataset collection
- Size classification during training
- Administrator model comparison
- Original model files and training script

The next separate update should handle the other paper-to-system mismatches, especially the model metrics, dataset count, password rules, default administrator password, HTTPS wording, and security claims.